FROM python:3.12-slim
WORKDIR /app
COPY pyproject.toml ./
COPY packages/backend ./packages/backend
COPY apps/worker ./apps/worker
RUN pip install --no-cache-dir .
ENV PYTHONPATH=/app/apps/worker:/app/packages/backend
